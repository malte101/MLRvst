Archived documentation that is no longer part of the active top-level doc set.

- `build-guides/`
  Legacy or superseded build instructions. The canonical build guide is `../BUILD.md`.
- `notes/`
  Historical implementation notes and versioned change writeups kept for reference.
